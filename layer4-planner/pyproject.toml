[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "layer4-planner"
version = "0.1.0"
description = "Planning Agent — Layer 4 of Ticket-to-PR"
requires-python = ">=3.12"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["layer4_planner"]
