[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "layer6-validator"
version = "0.1.0"
description = "Validation & Test Runner — Layer 6 of Ticket-to-PR"
requires-python = ">=3.12"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["layer6_validator"]
